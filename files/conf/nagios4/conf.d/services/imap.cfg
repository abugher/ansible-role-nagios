define service {
  hostgroup_name                mail-server
  service_description           IMAP
  check_command                 check_imap_version
  use                           generic-service
  notification_interval         0
  contact_groups                admins
}
